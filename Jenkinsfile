pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'hostname'
                sh 'pwd'
                sh 'gradle --version'
            }
        }
    }
}